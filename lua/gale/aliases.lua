local alias = require("gale.utils").add_alias

alias("CdProject", "cdp")
alias("Neogit", "git")

-- Workarounds for my dumb fingers
alias("qa", "Qa")
alias("qa", "QA")
alias("q", "Q")
alias("w", "W")
